#!/bin/bash -x

echo "########################################################################"
echo "#                                 misc                                 #"
echo "########################################################################"

dpkg-query -Wf '${Package;-40}${Priority}\n' | grep -v essential

apt-get clean
apt-get autoclean
apt-get autoremove
rm -rf /var/lib/apt/lists/*
rm -rf /usr/lib/locale/*
